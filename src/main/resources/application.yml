spring:
  datasource:
    url: jdbc:mysql://localhost:3306/restApitest?createDatabaseIfNotExist=true
    username: root
    password: Tanu6601
    
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect  